on:
  push: 


jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: test-script
        run: echo "$repo_name"
        uses:
          repo_name: ${{ github.event.repository.name }}