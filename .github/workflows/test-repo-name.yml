on:
  push: 


jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: test-script
        run: echo "$repo_name"
        with:
          repo_name: ${{ github.event.repository.name }}